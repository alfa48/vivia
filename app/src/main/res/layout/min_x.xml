<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#020B25"
    tools:context=".MainActivity">

    <!-- Título -->
    <TextView
        android:id="@+id/textViewVivia"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:layout_marginTop="24dp"
        android:text="vív ia"
        android:textColor="#FFFFFF"
        android:textSize="22sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />



    <!-- Ícones topo direito -->
    <LinearLayout
        android:id="@+id/topIcons"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:layout_marginEnd="32dp"
        android:orientation="horizontal"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/btnSettings"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginEnd="16dp"
            android:contentDescription="Configurações"
            android:src="@drawable/ic_settings"
            app:tint="#FFFFFF" />

        <ImageView
            android:id="@+id/btnNewChat"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:contentDescription="Novo Chat"
            android:src="@drawable/ic_edit"
            app:tint="#FFFFFF" />
    </LinearLayout>

    <!-- Texto principal -->
    <TextView
        android:id="@+id/textResponse"
        style="@style/viviaResponse"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="100dp"
        android:gravity="center_vertical"
        android:text="Oi, queres fazer o que?"
        android:textColor="#ffff"
        android:textSize="34sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/textViewVivia"
        app:layout_constraintWidth_percent="0.85" />

    <!-- Cards informativos -->
    <LinearLayout
        android:id="@+id/infoContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="32dp"
        app:layout_constraintTop_toBottomOf="@id/textResponse"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_percent="0.85">

        <TextView
            style="@style/InfoCard"
            android:gravity="center_vertical"
            android:text="Gerencie as definções do teu dispositivo." />

        <TextView
            style="@style/InfoCard"
            android:gravity="center_vertical"
            android:text="Controle os recuros do teu telefone por voz." />

        <TextView
            style="@style/InfoCard"
            android:gravity="center_vertical"
            android:text="Vívi_ia, easy." />
    </LinearLayout>

    <!-- ScrollView para resposta da IA -->

    <!-- Campo de entrada -->
    <androidx.constraintlayout.widget.ConstraintLayout

        style="@style/InfoCard"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal|center_vertical"
        android:orientation="horizontal"
        android:padding="10dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <ImageView
            android:id="@+id/btnMic"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginEnd="8dp"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Microfone"
            android:src="@drawable/ic_mic"
            app:layout_constraintBottom_toBottomOf="@+id/editTextCommand"
            app:layout_constraintEnd_toStartOf="@+id/editTextCommand"
            app:layout_constraintTop_toTopOf="@+id/editTextCommand"
            app:tint="#FFFFFF" />

        <EditText
            android:id="@+id/editTextCommand"
            android:layout_width="240dp"
            android:layout_height="90dp"
            android:background="@drawable/bg_input_rounded"
            android:hint="Message"
            android:inputType="text"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:textColor="@android:color/white"
            android:textColorHint="#77FFFFFF"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:id="@+id/btnSend"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Microfone"
            android:src="@drawable/ic_send"
            app:layout_constraintBottom_toBottomOf="@+id/editTextCommand"
            app:layout_constraintStart_toEndOf="@+id/editTextCommand"
            app:layout_constraintTop_toTopOf="@+id/editTextCommand"
            app:tint="#FFFFFF" />
    </androidx.constraintlayout.widget.ConstraintLayout>


    <!-- Botão de microfone -->

    <!-- Indicador de carregamento -->
    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
